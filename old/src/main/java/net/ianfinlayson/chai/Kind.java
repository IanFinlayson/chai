package net.ianfinlayson.chai;

// a simple type -- just what kind is it, but does not include
// specifics ie what kind of list/tuple/etc.

public enum Kind {
    INT,
    FLOAT,
    BOOL,
    STRING,
    LIST,
    DICT,
    TUPLE,
    SET,
    FUNCTION
}

