def makeWindow(title = "", width = 1024, height = 768, full = False):
    pass


def main():
    makeWindow("Game", 800, 600, False)
    makeWindow()
    makeWindow("My Program")
    makeWindow(title = "Tetris", full = True)

